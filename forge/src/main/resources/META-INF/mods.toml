modLoader="javafml"
license="${mod_license}"
loaderVersion="${forge_loader_version_range}"
issueTrackerURL="${mod_issues}"
displayURL="${mod_page}"
logoFile="${mod_id}_logo.png"

[[mixins]]
config="exposure-common.mixins.json"

[[mixins]]
config="exposure-forge.mixins.json"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
authors="${mod_authors}"
description="${mod_description}"

[[dependencies.${mod_id}]]
modId="forge"
required=true
versionRange="${forge_version_range}"
ordering="NONE"
side="BOTH"

[[dependencies.${mod_id}]]
modId = "minecraft"
type = "required"
versionRange = "${minecraft_version_range}"
ordering = "NONE"
side = "BOTH"

[[dependencies.${mod_id}]]
modId = "create"
type = "optional"
reason = "${create_version_range_reason}"
versionRange = "${create_version_range}"
ordering = "NONE"
side = "BOTH"