plugins {
    id 'java'
}

java {
    toolchain.languageVersion = JavaLanguageVersion.of(25)
}

// Common module - sources are included in neoforge module via sourceSets
// The actual build logic is in neoforge/build.gradle